#!/usr/bin/env ruby

$LOAD_PATH.unshift File.join(File.dirname(__FILE__), "..", "lib")

require 'image'

filename = ARGV.shift

i = Marshal.load(File.read(filename))

puts "Mask:"
puts i.mask_image.formatted
puts
puts "Image:"
puts i.formatted
